{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://architecturetokens.org/schema/architecture-model.schema.json",
  "title": "Architecture Model",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "id", "libraries", "components", "relationships"],
  "properties": {
    "kind": {"const": "architecture-model"},
    "id": {"type": "string", "minLength": 1},
    "name": {"type": "string"},
    "libraries": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*@[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"}, "uniqueItems": true},
    "components": {"type": "array", "items": {"$ref": "#/$defs/component"}},
    "relationships": {"type": "array", "items": {"$ref": "#/$defs/relationship"}}
  },
  "$defs": {
    "ref": {"type": "string", "pattern": "^[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)*$"},
    "application": {"type": "object", "additionalProperties": false, "required": ["token"], "properties": {"token": {"$ref": "#/$defs/ref"}, "value": {}}},
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "tokens"],
      "properties": {
        "id": {"type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_.-]*$"},
        "type": {"$ref": "#/$defs/ref"},
        "tokens": {"type": "array", "items": {"$ref": "#/$defs/application"}},
        "configuration": {"type": "object"},
        "metadata": {"type": "object"}
      }
    },
    "relationship": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type", "from", "to", "tokens"],
      "properties": {
        "id": {"type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_.-]*$"},
        "type": {"$ref": "#/$defs/ref"},
        "from": {"type": "string"},
        "to": {"type": "string"},
        "tokens": {"type": "array", "items": {"$ref": "#/$defs/application"}},
        "configuration": {"type": "object"},
        "metadata": {"type": "object"}
      }
    }
  }
}
