{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://architecturetokens.org/schema/token-library.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "namespace", "domains", "version", "name", "tokens"],
  "properties": {
    "kind": {"const": "token-library"},
    "namespace": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"},
    "domains": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"}, "uniqueItems": true},
    "version": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"},
    "name": {"type": "string", "minLength": 1},
    "description": {"type": "string"},
    "tokens": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/token"}}
  },
  "$defs": {
    "ref": {"type": "string", "pattern": "^[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:\\.[a-z0-9][a-z0-9-]*)*$"},
    "token": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "name", "description"],
      "properties": {
        "id": {"type": "string", "pattern": "^[a-z][a-z0-9-]*(?:\\.[a-z0-9][a-z0-9-]*)*$"},
        "kind": {"enum": ["component-type", "relationship-type", "applied"]},
        "name": {"type": "string", "minLength": 1},
        "description": {"type": "string", "minLength": 1},
        "appliesTo": {"type": "object"},
        "valueSchema": {"type": "object"},
        "requires": {"type": "array", "items": {"$ref": "#/$defs/ref"}},
        "conflicts": {"type": "array", "items": {"$ref": "#/$defs/ref"}},
        "mappings": {"type": "object"}
      }
    }
  }
}
