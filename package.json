{
  "name": "@architecture-tokens/website",
  "version": "0.1.0",
  "private": true,
  "description": "The public website for Architecture Tokens.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "types:tracked": "git ls-files --error-unmatch worker-configuration.d.ts > /dev/null",
    "types:check": "wrangler types --check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run types:tracked && npm run types:check && npm run typecheck && npm test && npm run deploy:dry",
    "verify:production": "node scripts/verify-production.mjs",
    "deploy:dry": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "@fontsource/ibm-plex-mono": "5.3.0",
    "@fontsource-variable/ibm-plex-sans": "5.3.0",
    "@types/node": "26.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  },
  "license": "Apache-2.0"
}
